# ========================================
# 🔧 ENVIRONMENT VARIABLES - DEVELOPMENT
# ========================================
# Copy this file to .env.local and update the values

# Database Configuration
# For Development: Use SQLite (easier setup)
DATABASE_PROVIDER="sqlite"
DATABASE_URL="file:./dev.db"
DIRECT_URL="file:./dev.db"

# For Production/PostgreSQL: Use these instead
# DATABASE_PROVIDER="postgresql"
# DATABASE_URL="postgresql://username:password@localhost:5432/dietas_db?schema=public&sslmode=require"
# DIRECT_URL="postgresql://username:password@localhost:5432/dietas_db?schema=public&sslmode=require"

# NextAuth Configuration
NEXTAUTH_URL="http://localhost:3000"
# Generate a secure secret key for production:
# Run: openssl rand -base64 32
# Or visit: https://generate-secret.vercel.app/32
NEXTAUTH_SECRET="your-secret-key-here-change-in-production"

# API Keys (Ejemplos - reemplazar con valores reales)
OPENAI_API_KEY=""
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
GITHUB_ID=""
GITHUB_SECRET=""

# Email (SendGrid, Resend, etc.)
EMAIL_SERVER=""
EMAIL_FROM="noreply@app-dietas.com"
SENDGRID_API_KEY=""

# Storage (S3, Cloudinary, etc.)
CLOUDINARY_URL=""
AWS_ACCESS_KEY_ID=""
AWS_SECRET_ACCESS_KEY=""
AWS_REGION=""
S3_BUCKET=""

# Monitoring
SENTRY_DSN=""
VERCEL_ANALYTICS_ID=""

# Feature Flags
ENABLE_BETA_FEATURES="false"
MAINTENANCE_MODE="false"

# App Configuration
NODE_ENV="development"
NEXT_PUBLIC_API_URL="http://localhost:3000/api"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
APP_NAME="Dietas NutriMed"

# ========================================
# 📋 SETUP INSTRUCTIONS
# ========================================
# 1. Copy this file to .env.local
# 2. For development with SQLite: Use the DATABASE_* variables above
# 3. For PostgreSQL: Uncomment and update the PostgreSQL DATABASE_* variables
# 4. Generate NEXTAUTH_SECRET: openssl rand -base64 32
# 5. Configure OAuth providers if needed
# 6. Run: npm install && npm run db:push && npm run dev

# ========================================
# 🚀 PRODUCTION DEPLOYMENT
# ========================================
# For Vercel deployment:
# 1. Use the values from .env.production
# 2. Set up PostgreSQL database (Vercel Postgres, Supabase, etc.)
# 3. Add all environment variables to Vercel dashboard
# 4. Run database migrations after deployment