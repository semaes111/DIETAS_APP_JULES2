# ========================================
# 🚀 PRODUCTION ENVIRONMENT VARIABLES
# ========================================
# Copy this to .env.production.local for Vercel deployment

# Database Configuration (PostgreSQL for production)
DATABASE_PROVIDER="postgresql"
DATABASE_URL="postgresql://username:password@localhost:5432/dietas_db?schema=public&sslmode=require"
DIRECT_URL="postgresql://username:password@localhost:5432/dietas_db?schema=public&sslmode=require"

# NextAuth Configuration
NEXTAUTH_URL="https://your-app-domain.vercel.app"
NEXTAUTH_SECRET="generate-a-secure-secret-key-32-characters-minimum"

# OAuth Providers (optional)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Admin Configuration
ADMIN_EMAIL="admin@yourdomain.com"
ADMIN_PASSWORD="SecureAdminPassword123!"

# App Configuration
NODE_ENV="production"
NEXT_PUBLIC_API_URL="https://your-app-domain.vercel.app/api"
NEXT_PUBLIC_APP_URL="https://your-app-domain.vercel.app"

# Feature Flags
ENABLE_BETA_FEATURES="false"
MAINTENANCE_MODE="false"

# ========================================
# 📋 VERCEL DEPLOYMENT INSTRUCTIONS
# ========================================
# 1. Add all these environment variables to your Vercel dashboard
# 2. Set up PostgreSQL database (Vercel Postgres, Supabase, PlanetScale, etc.)
# 3. Update DATABASE_URL and DIRECT_URL with your database connection strings
# 4. Generate NEXTAUTH_SECRET: openssl rand -base64 32
# 5. Deploy to Vercel
# 6. Database will be automatically initialized on first deployment