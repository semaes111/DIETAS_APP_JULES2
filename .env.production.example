# ========================================
# 🚀 PRODUCTION ENVIRONMENT VARIABLES
# ========================================
# Use these settings for Vercel deployment

# Database Configuration - REQUIRED
# Use PostgreSQL for production (Vercel Postgres, Supabase, PlanetScale, etc.)
DATABASE_PROVIDER="postgresql"
DATABASE_URL="postgresql://username:password@host:5432/database_name?schema=public&sslmode=require"
DIRECT_URL="postgresql://username:password@host:5432/database_name?schema=public&sslmode=require"

# NextAuth Configuration - REQUIRED
NEXTAUTH_URL="https://your-app-name.vercel.app"
# Generate using: openssl rand -base64 32
NEXTAUTH_SECRET="your-secure-production-secret-32-chars-minimum"

# Application Configuration - REQUIRED
NEXT_PUBLIC_APP_URL="https://your-app-name.vercel.app"
NEXT_PUBLIC_API_URL="https://your-app-name.vercel.app/api"
APP_NAME="Dietas NutriMed"
NODE_ENV="production"

# Google OAuth - OPTIONAL (for Google login)
GOOGLE_CLIENT_ID="your-google-client-id.googleusercontent.com"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# Admin Configuration - OPTIONAL
ADMIN_EMAIL="admin@yourdomain.com"

# Build Configuration
SKIP_ENV_VALIDATION="false"

# Feature Flags
ENABLE_BETA_FEATURES="false"
MAINTENANCE_MODE="false"

# Monitoring - OPTIONAL
SENTRY_DSN=""
VERCEL_ANALYTICS_ID=""

# ========================================
# 📋 VERCEL DEPLOYMENT INSTRUCTIONS
# ========================================
# 1. Set up database (Vercel Postgres recommended):
#    - Go to Vercel dashboard
#    - Create new Postgres database
#    - Copy the DATABASE_URL and DIRECT_URL from Vercel
#
# 2. Generate NEXTAUTH_SECRET:
#    - Run: openssl rand -base64 32
#    - Or use: https://generate-secret.vercel.app/32
#
# 3. Add all environment variables to Vercel:
#    - Go to Project Settings > Environment Variables
#    - Add each variable above with your actual values
#    - Set Environment: Production, Preview, Development (as needed)
#
# 4. Deploy:
#    - Push to GitHub
#    - Vercel will auto-deploy
#    - Database migrations will run automatically
#
# ========================================
# 🗄️ DATABASE SETUP
# ========================================
# After first deployment:
# 1. Visit: https://your-app-name.vercel.app/api/admin/init-db (POST request)
# 2. This will initialize the database schema
# 3. Optionally seed with sample data via the admin panel
